const config={
    appURL:String(import.meta.VITE_API_URL),
    // projectID:String(import.meta.VITE_PROJECT_ID),
    // databaseID:String(import.meta.VITE_DATABASE_ID),
    // bucketID:String(import.meta.VITE_BUCKET_ID),
    // collectionID:String(import.meta.VITE_COLLECTION_ID)
}
export default config